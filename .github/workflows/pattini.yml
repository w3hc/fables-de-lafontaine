name: Run Pattini

on:
  push:
    branches: "*"

jobs:
  on_merge_pull_request:
    runs-on: ubuntu-latest
    steps:
      - name: Pattini app
        uses: w3hc/pattini@vTest
        with:
          issue_number: "13"
          private_key: '0x...'
