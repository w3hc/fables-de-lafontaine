name: Run Pattini

on:
  push:
    branches: "*"

jobs:
  on_merge_pull_request:
    runs-on: ubuntu-latest
    steps:
      - name: Pattini app
        uses: w3hc/pattini@vTest
        with:
          param1: '12'
          param2: '0x...'
