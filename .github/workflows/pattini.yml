name: Run Pattini

on:
  push:
    branches: "*"

jobs:
  on_merge_pull_request:
    runs-on: ubuntu-latest
    steps:
      - name: Pattini app
        uses: w3hc/pattini@vTest
        with:
          ISSUE_NUMBER: ${{ github.event.push.head_commit.message }}
          PRIVATE_KEY: "0x123"
